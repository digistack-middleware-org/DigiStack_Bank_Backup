# INCIDENT TICKET — INC-v6-001

| Field | Value |
|---|---|
| Raised by | Automated monitoring / on-call admin |
| Priority | **High** |
| Time raised \<current time\> |

---

## Summary

Node02 cluster member behaving unexpectedly after routine configuration change.
Sync operation completed without expected confirmation.

---

## Symptoms Observed

1. **Admin Console → Nodes page** shows `dsb-node02` node status as **Unavailable** (grey icon, not green).
2. Clicking **Synchronize** for `dsb-node02` returned a **partial or error response** instead of
   `"Synchronization completed successfully."`
3. The JVM argument `-Dv6.fault.drill=true` added to the node02 application server
   **does not appear** in node02's local repository — the change is visible in the DMgr console
   but has **not reached the node**.
4. The application on node02 is **still serving traffic through IHS** — end users are not reporting errors yet.
   Home page, Login, Dashboard, Deposit all appear functional:
   `http://192.168.10.20/digistack-bank/Home`
5. A junior administrator reports: *"I tried restarting the application from the Admin Console and got an
   error — the console said the operation could not be sent to the node."*
6. wsadmin command attempted:

   ```python
   AdminControl.invoke(
       AdminControl.completeObjectName('type=NodeSync,node=<NODE_NAME>,*'),
       'sync'
   )
